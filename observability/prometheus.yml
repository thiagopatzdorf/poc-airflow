global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: poc
    node: honda

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: airflow
    static_configs:
      - targets: ["statsd-exporter:9102"]
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

